#ifndef SENSORS_H
#define SENSORS_H

// Define an accelerometer struct
typedef struct accelerometer{
    // Accelerometer parameters

} accelerometer;

// Define a gyroscope struct
typedef struct gyroscope{
    // Gyroscope parameters

} gyroscope;

#endif